# =============================================================================
# INFLUXDB STACK SYNC TOOLS - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and customize for your environment
# 
# Usage: cp .env.example .env && nano .env
# =============================================================================

# Home Assistant Connection Settings (for Addon Mode)
HA_HOSTNAME=homeassistant.local
HA_SSH_USER=root
HA_SSH_PORT=22
HA_SSH_KEY=~/.ssh/id_rsa
HA_SSH_PASSWORD=

# Container Settings
LOCAL_CONTAINER_NAME=influxdb-stack-test
REMOTE_CONTAINER_NAME=addon_local_influxdb_stack

# Sync Settings
SYNC_BACKUP_DIR=./sync-backups
EXTRACTED_DIR=./ssh-extracted-configs
VERBOSE=false
DRY_RUN=false

# =============================================================================
# CONFIGURATION NOTES
# =============================================================================
# 
# HA_HOSTNAME: Your Home Assistant hostname or IP address
#   - Use homeassistant.local for mDNS discovery
#   - Use IP address like 192.168.1.100 for direct connection
#   - Use custom hostname if you've configured one
#
# HA_SSH_USER: SSH username for Home Assistant
#   - Usually 'root' for SSH addon
#   - Check your SSH addon configuration
#
# HA_SSH_KEY: Path to SSH private key
#   - Generate with: ssh-keygen -t rsa -b 4096 -f ~/.ssh/ha_key
#   - Add public key to Home Assistant SSH addon
#
# LOCAL_CONTAINER_NAME: Name of local test container
#   - Created by ./test/build.sh
#   - Must match container name in test scripts
#
# REMOTE_CONTAINER_NAME: Name of addon container in Home Assistant
#   - Usually follows pattern: addon_local_influxdb_stack
#   - Check with: docker ps | grep influxdb
#
# VERBOSE: Enable detailed logging
#   - Set to true for debugging
#   - Set to false for normal operation
#
# DRY_RUN: Preview changes without applying them
#   - Set to true to see what would be done
#   - Set to false to apply changes
# =============================================================================
